# H100/CUDA environment dependencies for the retrieval training task.
# Install a CUDA-matched PyTorch wheel from the official PyTorch index before using this file when needed.
torch>=2.1
hydra-core>=1.3
omegaconf>=2.3
numpy>=1.23
tqdm>=4.64
tensorboard>=2.12
pyyaml>=6.0
# Optional native HPX regridding; training falls back to lat-lon when unavailable.
earth2grid; platform_system == "Linux"
python-dateutil>=2.8
click>=8.0
dask>=2023.1
scipy>=1.9
matplotlib>=3.6
einops>=0.6
timm>=0.9
torchvision>=0.16
xarray>=2023.1
pyrootutils>=1.0
